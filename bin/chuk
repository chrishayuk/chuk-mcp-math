#!/usr/bin/env python3
"""Main CLI entry point for CHUK MCP Math."""

import sys
import os
from pathlib import Path

# Add src to path
src_path = Path(__file__).parent.parent / 'src'
sys.path.insert(0, str(src_path))

from chuk_mcp_math.cli.main import cli

if __name__ == '__main__':
    cli()