#!/usr/bin/env python3
# Auto-generated CLI wrapper for prime_count
import sys
import os
import logging

# Suppress INFO logs for clean CLI output
logging.getLogger('chuk_mcp_math').setLevel(logging.WARNING)

sys.path.insert(0, "/Users/christopherhay/chris-source/chuk-mcp-math/src")

from chuk_mcp_math.cli import CLIWrapper
from chuk_mcp_math.number_theory.primes import prime_count

if __name__ == "__main__":
    wrapper = CLIWrapper(prime_count, "prime_count")
    sys.exit(wrapper.run(sys.argv[1:]))
